import 'package:flutter/material.dart';
import 'auth_screen.dart';

/// Routes to AuthScreen for the Android application.
class PlatformAuthScreen extends StatelessWidget {
  const PlatformAuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AuthScreen();
  }
}

